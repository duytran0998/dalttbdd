<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tvd.pro.studentsmanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_school_top"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ChangePassStudentActivity" />
        <activity android:name=".activities.TeacherActivity" />
        <activity android:name=".activities.TeacherLoginActivity" />
        <activity android:name=".activities.StudentLoginActivity" />
        <activity android:name=".activities.StudentActivity" />
        <activity android:name=".activities.ChangePassTeacherActivity"></activity>
    </application>

</manifest>